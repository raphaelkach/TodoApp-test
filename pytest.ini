[pytest]
# Python-Pfad (wichtig für imports)
pythonpath = .

# Testpfad
testpaths = tests

# Standard-Optionen für 80%+ Coverage
addopts =
    -v
    --cov=model
    --cov=controller
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80